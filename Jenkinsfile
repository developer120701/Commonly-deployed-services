pipeline {
    agent any
    stages {
        stage('Build') {
            steps{
                git branch: 'main', url: 'https://github.com/LamTruong-SystemAdmin/Commonly-deployed-services.git'
            }
        }
    }
}